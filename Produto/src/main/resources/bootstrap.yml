spring:
  application:
    name: 'produto'
  datasource:
    initialization-mode: always
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888
#      uri: http://config-server:8888
      