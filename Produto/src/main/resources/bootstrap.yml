spring:
  application:
    name: 'produto'
  datasource:
    initialization-mode: always
  profiles:
    active: default
  cloud:
    config:
      uri: http://config_server:8888
#      uri: http://localhost:8888